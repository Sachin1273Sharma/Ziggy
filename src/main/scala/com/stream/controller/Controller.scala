package com.stream.controller
import com.stream.actor.ActorProvider
import com.stream.database.model.Customer

class Controller(actors : ActorProvider)
{
  def registerCustomer(customer : Customer): Unit = {
      
  }
}
